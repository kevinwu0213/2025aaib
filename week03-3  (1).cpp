#include <stdio.h>

int main() {
    // 電腦的 for 迴圈，從 0 開始
    for (int i = 0; i < 5; i++) {
        printf("%d", i);
    }

    printf("\n"); // 換行

    // 人的 for 迴圈，從 1 開始
    for (int i = 1; i <= 5; i++) {
        printf("%d", i);
    }

    return 0;
}
